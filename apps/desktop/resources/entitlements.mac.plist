<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!-- Electron requires these two under hardened runtime because V8 uses
         JIT-compiled code and the app loads dylibs from outside the signed
         bundle at runtime. -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Needed if the app ever dlopen()s a third-party library (e.g. ffmpeg
         from a user-provided path). Safe to grant. -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- Screen recording requires this plus a user permission grant. Dreambyte
         uses getDisplayMedia() via the native macOS system picker. -->
    <key>com.apple.security.device.camera</key>
    <true/>
    <key>com.apple.security.device.audio-input</key>
    <true/>
  </dict>
</plist>
