'use client'

// Top-level Media tab: pure asset library (upload + browse + reuse). The
// "create new with AI" flow now lives inside the Layers tab's Media sub-tab,
// next to Transitions / Avatar / Effects, so generation stays scene-scoped.
import GalleryPanel from './media/GalleryPanel'

export default GalleryPanel
